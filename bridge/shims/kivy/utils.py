"""`katrain.core.engine` lit `platform` pour choisir comment lancer KataGo.

L'application ne cible que macOS, donc la valeur est constante. Le nom `macosx`
est celui qu'emploie Kivy, et c'est celui qu'`engine.py` compare.
"""

platform = "macosx"
